kind = "web"
previewPath = "/"
title = "Proof of Human Platform"
version = "1.0.0"
id = "artifacts/proof-of-human"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25723

[services.development]
run = "pnpm --filter @workspace/proof-of-human run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/proof-of-human", "run", "build" ]
publicDir = "artifacts/proof-of-human/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25723"
BASE_PATH = "/"
